// postcss.config.mjs
const config = {
  plugins: {
    tailwindcss: {}, // Usa el plugin 'tailwindcss' directamente para v3.x
    autoprefixer: {}, // Asegúrate de tener autoprefixer para prefijos de CSS
  },
};
export default config;
